# Biohub — Cell Tracking During Development (Kaggle)

<!-- BEGIN AUTO-CHECKPOINT (generated by scripts/render_checkpoint.py — edit STATE.json, not this block) -->
## Active checkpoint (single source of truth: `STATE.json`)

- **Active experiment:** `exp_055_original_score_joint_bootstrap_smoke_fix`
- **Controller state (from `experiments/exp_055_original_score_joint_bootstrap_smoke_fix/experiment.json`):** KEEP
- **Phase:** EXP060_LAUNCHED_MANUAL_PUSH_AWAITING_COMPLETION
- **Leaderboard submission:** NOT authorized
- **Summary:** ACTIVE: exp_060_deepcenter_safe_div_threshold_sweep is LAUNCHED on Kaggle (kernel lingxd/biohub-exp060-deepcenter-safe-div-threshold-sweep v1, manual push OUTSIDE the controller by explicit user authorization on 2026-09-18, GPU running, awaiting completion). It sweeps the DeepCenter safe-division accept threshold {0.20 control / 0.18 / 0.22} on the FROZEN 0.947 parent pipeline (repro_059), pinning everything else; division is the bottleneck (the 0.20 gate accepts 316 / REJECTS 414 candidate divisions on the test movies, so 0.18/0.22 WILL move the accepted set; 0.18 is the primary arm, division being FN-dominated at held-out 3/1/9). It produces submission_thr{020,018,022}.csv + exp060_telemetry.json + a gated metrics.json; the 0.20 arm must byte-reproduce the parent submission SHA d34533806b3153ddd4f33f3bbc1dea70af2d5406bb1ea48e42c135a97c213f60 (the real-pipeline proof). NO auto LB submission. Parent context: NEW BEST Public LB 0.947 (repro_059, authenticated submission 56313491) = a verbatim copy of a public 0.947 notebook on the SAME Pilkwang checkpoints as our 0.944, so the +0.003 is 100% post-processing/TTA (DeepCenter TTA + secondary edge TTA + safe-div threshold 0.25->0.20 + held-out PP-sweep tight55), not causally isolated; adopted as working parent. exp_060 reached strategy CONSENSUS (3 Codex gpt-6-astra rounds) + implementation verified across 12 Codex rounds; it was launched by manual push because the controller's formal admission (request_codex_review.py) returned REVISE on a CIRCULAR objection (it demands verifying the REAL GPU postprocessing pipeline locally, which needs the very GPU run the review gates) after every fixable finding was fixed and re-confirmed. Standing lesson: train16 proxy gains twice failed to transfer (exp_055 0.942==0.942, exp_057 0.944==0.944); the LB (now unlimited submissions) is the held-out truth.
- **Next action:** WAIT for the exp_060 Kaggle run to complete (do NOT poll; the user will report). See HANDOUT.md '>>> NEXT STEP <<<' (authoritative). ON COMPLETION: (1) kaggle kernels output lingxd/biohub-exp060-deepcenter-safe-div-threshold-sweep; (2) verify metrics.json exp060_threshold_sweep_integrity_passed==true AND arm_submission_sha256.thr020 == d34533806b3153ddd4f33f3bbc1dea70af2d5406bb1ea48e42c135a97c213f60 (0.20 byte-parity = real-pipeline proof); if False/mismatch the run is INVALID. (3) Analyze exp060_telemetry.json delta_vs_control.thr018/.thr022 (division forks/edges added/removed vs 0.20, full node IDs) + candidate_counts. (4) SEPARATE user authorization: submit the 0.18 (primary) / 0.22 arm to the Public LB -- competition is NOTEBOOK-ONLY, so an arm needs a variant notebook whose OUTPUT submission.csv IS that arm (add an env flag copying exp060/submission_thrXXX.csv to SUBMISSION_PATH, re-run, competition_submit_code). Decision rule vs 0.947: >=0.948 adopt as new parent; ==0.947 unresolved below displayed precision (pivot to next division lever, e.g. deferred exp_061 corrected Z-reflection DeepCenter TTA); <=0.946 revert, retain repro_059. Do NOT relaunch/submit without user go-ahead. Deferred exp_058 next-action below is historical: User authorized ONE minimal GPU diagnostic export for A0 Parts 2-3. A0 Part 1 done zero-GPU (edge upper-bounds; exact scorer reconstruction): edge layer not exhausted but the big edge lever = fragmentation already LB-flat via exp_055; wrong-association only +0.0015; node pruning HURTS; division retains most headroom. Parts 2-3 need GT graphs = a GPU export. Diagnostic experiment proposal docs/research/exp058_a0_division_diagnostic_proposal.md is at v2 after Codex challenge v1 (REVISE, docs/research/exp058_a0_codex_challenge_v1.md). It is PREDICTION-PRESERVING (shadow parity + test-submission SHA 0319ba6d + exact val_049 3/8/9 reproduction), reuses val_049 frozen GEFFs (rerun only postprocessing+DeepCenter+GT-map+scorer, ~<=1.0 GPU hr with a measured estimate + hard stop; a full rerun is ~1.17 hr and is excluded), corrected the safe_division 14-step gate order + missing_bypass + downstream tracing, made fork accounting scorer-faithful (only aggregate is official TP/FP/FN; reproduce evaluate_divisions), added a GT-centric coverage table for all 12 GT divisions, and pre-registers exploratory-only feature discipline. UPDATE 2026-09-17: the diagnostic was BUILT (full re-run), a first formal Codex admission returned BLOCK, a v3 revision fixed the contract-level findings, the v3 snapshot was REBUILT, and a FRESH Codex admission on the v3 snapshot returned BLOCK AGAIN (see exp058_build.admission_review_2). The gate held: NO Kaggle launch. Codex confirmed the contract fixes but found 7 deeper P2/P3 semantic-correctness defects (P2 coverage on wrong graph snapshot + wrong stage order; P3 not reproducing the scorer's weakly-connected-component credit; no-op ambiguity flag; missing feature join; incomplete downstream-stage tracing; non-fail-closed config guard; weak completeness/NaN checks) plus the standing 'not-at-CONSENSUS' governance blocker. NEXT: a substantial SECOND analysis revision per exp058_build.remaining, then a reconciled strategy CONSENSUS, snapshot rebuild, and a fresh Codex admission PASS before any smoke/reservation/launch. No LB submission (diagnostic). Six protected GPU hours + >=10 percent weekly allowance preserved. Governance: train16 is exhausted exploratory calibration; any A2 still needs new labeled specimens or a pre-registered one-shot external test.

_Generated from `STATE.json` (updated 2026-09-18T00:00:00+00:00). Do not hand-edit this block; edit `STATE.json` and rerun `python scripts/render_checkpoint.py`. Full authorization detail lives once in `GOAL.md`._
<!-- END AUTO-CHECKPOINT -->

The current authority is `STATE.json`, the active experiment record, the current
workflow block in `GOAL.md`, and `docs/research/PROJECT_HANDOFF.md`. Older dated
sections below are historical evidence and cannot override those sources.

## Startup directive for Claude Code

On every new Claude Code session, read these files in order before proposing or
editing anything:

1. `STATE.json`.
2. `experiments/exp_055_original_score_joint_bootstrap_smoke_fix/experiment.json`
   while exp_055 remains the active experiment.
3. `.private/current/CONTINUATION.md`.
4. `docs/research/PROJECT_HANDOFF.md`.
5. The current workflow section of `GOAL.md` and the role contract in `AGENTS.md`.

exp_055 is now terminal `KEEP` and has been independently audited (2026-09-15);
the single check/collection is complete and must not be repeated. Do not
relaunch, re-collect, rebuild, request another review of it, submit to the
leaderboard, or promote. The exact input/output graphs, score rows, solver
receipt, `metrics.json`, and submission integrity were verified against the
zero-GPU `local_052` original-score pilot; the audit is
`.private/research/exp055_completed_analysis_2026-09-15.md`.

The project is in the Valid-KEEP branch. Claude Code's active task is to author a
versioned strategy proposal (an independent exact reproduction of the
original-score gain, or a bounded structural proposal). Codex must challenge it,
Claude must revise it, and no implementation or execution starts until the record
says `CONSENSUS` and the future experiment clears a fresh Codex review. KEEP does
not authorize promotion or leaderboard submission.

## Current workflow

The canonical operational handoff is `docs/research/PROJECT_HANDOFF.md`.
Claude Code is the primary strategy author and implementation lead. It must
propose the actual parent, falsifiable hypothesis, exact change, validation,
budget, artifacts, risks, and rollback/stop rule. Codex independently challenges
the strategy and reviews the implementation for provenance, leakage, graph
semantics, numerical stability, reproducibility, tests, and budget. Claude Code
revises until the versioned strategy record contains explicit `CONSENSUS`; a
`NO_CONSENSUS` record stops execution and returns the disagreement to the user.
For agreed authoring work, Claude Code may edit the repository by default; it is
read-only only while acting in an explicit review or audit role.

After consensus, existing experiment admission remains mandatory: fresh
experiment-specific review, snapshot smoke, tracked budget, explicit
parent/change/hypothesis, and controller gates. Future successor configs must
set `admission.require_codex_review: true` (optionally
`reviewer_provider: codex`), use `scripts/request_codex_review.py`, and record a
fresh Codex `PASS`; historical Claude-based configs and receipts, including
exp_055, remain valid evidence and are not rewritten. Codex's independent audit
is an additional gate. Late-stage, high-risk/high-reward and potentially
framework-changing experiments are encouraged when explicitly authorized, but
must remain bounded, reversible, fail-fast, leakage-free, reproducible, and
honest about local proxy versus Public LB evidence. No automatic leaderboard
submission or promotion is implied. The migrated controller accepts the Codex
admission field while remaining backward-compatible with historical configs and
records; historical Claude receipts are not rewritten.


## Superseded historical context

The material below records earlier checkpoints. It is useful for provenance but
contains former next actions that are no longer executable instructions.

`exp_040_public_0941_motion_ema` completed as KEEP at 0.9387332376874039,
+0.002755424445175847 over val_039. Independent raw-artifact audit passed. Both
specimen aggregates improved and division TP/FP/FN changed from 4/9/8 to 4/8/8.
The candidate is positive but `reproducible: false`, and video-level effects are
heterogeneous. Do not collect or launch exp_040 again. The proposed next step is
one exact independent reproduction, not yet authorized or created. No leaderboard
submission, formal promotion, or milestone v06 is authorized. Read the latest
continuation checkpoint; it overrides all older PAUSED/RUNNING text.

## Historical preparation record

The user selected `repro_038_public_0941_exact_copy` (confirmed Public LB 0.941,
submission 56044403 COMPLETE) as the working research parent. The authorized task
is `val_039_public_0941_train16`: preserve test inference exactly, establish the
frozen val_008 train16 validation baseline, bind the actual epoch-2 best.pt path/hash,
and report effective parameters plus per-video errors/stage statistics. No model
training or optimization is part of this step. Historical 0.933/EMA metrics are
controls, not a pass threshold. CURRENT_BEST.json is the legacy formal promotion
record; it must not redirect the working parent to 0.912.

The user previously requested no polling while waiting.
val_039 was historically observed RUNNING on lingxd/biohub-val039-public0941-train16, launched once at
2026-09-06T13:34:05Z after this experiment's Claude PASS and smoke PASS. Do not
request another review or launch again. Resume by checking and collecting that run.

Read GOAL.md, .private/current/CONTINUATION.md, .private/current/MEMORY.md and the
active experiment.json. These override all older plans below and the old auto-memory
files. Review the exact snapshot, builder and preflight/contract templates named in
the config. Frozen scorer functions and selector/runner are preserved by static checks;
runtime requires the exact parent submission SHA256 before and after validation.
The single fresh review must remain read-only and bounded. Exact model allowance is
unknown; no automatic retry after timeout or quota stop. Launch requires PASS and smoke.
Do not submit to LB, promote formally, start optimization or push during this task.

## Historical archive

Earlier checkpoints and superseded plans are preserved in `.private/archive/checkpoints/CLAUDE_before_train16_039.md`.
They are historical evidence and do not override this current handoff.
