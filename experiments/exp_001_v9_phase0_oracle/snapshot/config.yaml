schema_version: 1
experiment_id: exp_001_v9_phase0_oracle
parent: baseline_public_0912
hypothesis: Recoverable division edits have positive combined metric value on both specimens.
change:
  component: division_oracle_audit
  from: frozen 0.912 graph
  to: counterfactual oracle families A, B, and C
  variables_changed: 1

source_notebook: src/v9_division_aware.ipynb

validation:
  protocol: v9_phase0_oracle_train16_delta_only
  primary_metric: score
  seed: 0
  required_specimens: [44b6, 6bba]
  warning: Frozen feature extractors saw train videos; use deltas only.

budget:
  tier: 1
  expected_gpu_hours: 1.0

success:
  minimum_improvement: 0.002
  regression_threshold: -0.002
  per_specimen_max_regression: 0.0

evaluation:
  mode: gate
  gate_field: gate_0_passed

admission:
  require_claude_review: false
  methodology_valid: true
  require_reproducible_for_promotion: false

local:
  smoke_test:
    - "{python}"
    - scripts/validate_notebook.py
    - "{source_notebook}"
    - --require-metrics-contract

kaggle:
  owner: null
  owner_env: KAGGLE_USERNAME
  slug: biohub-v9-phase0-oracle
  title: Biohub v9 Phase 0 oracle audit
  competition_sources: [biohub-cell-tracking-during-development]
  dataset_sources: []
  required_dataset_sources: 4
  source_dataset_version_ids: [17751825, 17804310, 18187037, 18673646]
  enable_gpu: true
  enable_internet: true
  is_private: true
