I have enough to complete the review. The working tree is clean (the experiment is committed at `815cf5d`), so there is no pending diff â€” I reviewed the committed snapshot and the staged oracle module directly.

---

# Review â€” `diag_015_train16_candidate_constrained_oracle`

## Summary
A CPU-only (GPU-hours = 0), GT-informed counterfactual **oracle ceiling** for v9 Phase 0 GATE 0. It consumes the completed parent `diag_014` artifacts (final prediction `.geff` + bounded pre-ILP candidate export), applies oracle-correct atomic division edits over cumulative families A / A+B / A+B+C **constrained to edges that actually appear in the diag_014 candidate set**, and rescores with the pack's official metric to report per-specimen `Î”score = Î”adj + 0.1Â·Î”div_J`. The design is methodologically clean, the runtime self-verifies against the parent baseline, and the info-gain-to-cost ratio is excellent (a free gate on a ~30 h Phase-2 commitment). The principal risks are all external-artifact coupling that can only be exercised on Kaggle, plus one silent failure mode where an id/path mismatch would masquerade as "no division headroom."

## Methodology
- **One variable, testable, attributable (Q1):** `variables_changed: diagnostic_analysis_only`. No model runs, no training; it reads frozen `diag_014` outputs and rescores. The output is a per-family, per-specimen `Î”score` ceiling â€” a well-posed upper bound conditioned on the realizable candidate generator. This is a genuine refinement over `exp_001` (unconstrained oracle), not a duplicate: it answers "how much of the oracle ceiling survives when a classifier can only use candidate edges it actually sees?"
- **Protocol / leakage / domain split (Q2):** The 16-video split is *inherited* from the parent (`parent_metrics["specimen_metrics"][spec]["samples"]`), so the split is not silently changed and stays division-stratified. Both specimens are computed and reported separately; `gate_passed` requires ABC `Î”score > 0` on **both** 44b6 and 6bba. The GT-informed nature is intentional (it's an oracle ceiling, not a promotable score), and the train-leakage optimism warning is carried in the payload. Consistent with GOAL.md and the v9 plan's GATE 0.
- **Not contradicted by history (Q5):** This is not a scalar-threshold DeepCenter experiment (that branch is closed by diag_012/013); it is the ceiling audit those diagnostics were pointing toward. The Version-7 adj-FP landmine is exactly what ABC's `Î”adj` measures, so it is aligned with, not contradicted by, prior findings.

## Implementation risks
- **Hard path coupling to the parent (highest launch risk):** `prediction_dir = parent_root/"tracking_repo"/"predictions"/"unknown"/"unet_transformer_val"/"split_0"` and `candidate_dir = parent_root/"preilp_edge_audit"` are hardcoded. If `diag_014` did not persist the final prediction `.geff` at that exact path for all 16 names, the run aborts at the `is_dir()` check. `diag_014` was scoped as an *edge export*; confirm it also saved the post-ILP prediction graphs.
- **Silent zero-coverage â†’ false STOP:** `wanted` is built from `(gt_to_pred[mother], gt_to_pred[daughter])` original pred ids; candidate rows are keyed on diag_014 `(source_id, target_id)`. This assumes both live in the same node-id space. If they don't, every action is dropped by `if any(pair not in candidate_rows...)`, the oracle collapses to the baseline, `Î”score = 0`, and the gate FAILS â€” misattributing an id/path bug to "no headroom." There is no guard that any candidate coverage occurred. This is the most dangerous failure mode because it is indistinguishable from a real negative result in the payload.
- **Parent-schema coupling:** `_baseline_matches` and `run()` require `parent_metrics` to expose `specimen_metrics[spec]["adjusted_edge_jaccard"|"division_jaccard"|"samples"]` and top-level `primary_metric`. A field-name drift crashes late.
- **Guards genuinely verify effective config (Q4 â€” strong):** Cell 1 constructs a real polars DataFrame, checks `zarr>=3`/`polars.Float16`/compiled binary, and installs `--no-deps` to avoid ABI breakage â€” runtime verification, not prose. `baseline_reproduced` re-scores diag_014's predictions and asserts a `1e-10` match to the recorded specimen metrics, so "the frozen baseline is preserved" is *verified*, not asserted. The oracle module is staged as an extra file and cell 2 asserts exactly one copy â€” no stale embedded source. Node matching is edge-independent (centroid-distance), so reusing baseline `gt_to_pred` to build edge edits is valid, and each `_score` re-matches independently.
- **Output contract (Q3):** `metrics.json` carries `experiment_id`, `metrics.oracle_gate_passed` (bool), `primary_metric = min-specimen ABC Î”score`, and per-specimen `specimen_metrics[*].primary_metric`; cells 2â€“3 assert them and the config's `gate_field: oracle_gate_passed` matches. Contract looks complete.
- **Methodology nuance:** the machine gate is raw `Î”score > 0`; it applies no optimism discount, whereas GOAL.md/v9-plan call for a ~0.5Ã— local discount. `gate_passed = True` is necessary but not sufficient â€” the human reviewer must confirm the ABC delta magnitude clears the optimism margin on *both* specimens before authorizing Phase 1/2. The payload exposes raw deltas so this is possible.
- **Minor / non-blocking:** greedy `_select_nonconflicting` (sorted by mother id) is a conservative, non-optimal resolver â†’ may slightly *under*-state the ceiling (safe direction). Daughter-detectability is only implicit (`detectable`/`gt_divisions` counts) rather than an explicit fraction as in `exp_001`. `runtime_seconds` is hardcoded 0.0 (cosmetic; `wall_clock_seconds` is real).
- **Stale prose (documentation, not code):** GOAL.md still describes work "paused at diag_014 RUNNING," but results.json/EXPERIMENTS.md/GPU_BUDGET show diag_014 EVALUATED/KEEP at 2026-09-02T14:39Z (baseline 0.925252 reproduced). The pause boundary is cleared; the load-bearing check is the runtime `baseline_reproduced` guard, not the stale text â€” but GOAL.md should be updated.

## Budget
GPU cost = 0 (`enable_gpu: false`, tier 1, expected 0.0 h). Weekly remaining 18.87 h / reserve 6 h are untouched. No submission slot consumed. Info gain (Q6) is high: this is the mandatory realizability gate deciding whether the ~30 h Phase-2 spend is warranted. Excellent ROI; well within budget and quota policy.

## Required changes
1. **Add a candidate-coverage guard** so a zero-coverage id/path mismatch cannot be silently reported as a negative gate: after building `per_video`, assert that total `candidate_covered` (or total non-empty `candidates`) is > 0 for at least family A, and surface per-video coverage counts prominently. This is the one change I'd treat as required before a Kaggle launch.
2. **Verify the diag_014 artifact layout** before launch: confirm the prediction `.geff` exists at the hardcoded `tracking_repo/predictions/unknown/unet_transformer_val/split_0/` path and `preilp_edge_audit/<name>.jsonl.gz` exists for all 16 validation names, and that candidate `source_id/target_id` share the prediction graph's original node-id space.
3. **Confirm the parent metrics schema** (`specimen_metrics[*].adjusted_edge_jaccard/division_jaccard/samples`, top-level `primary_metric`) matches `diag_014`'s actual `metrics.json`.
4. **Recommended (not blocking):** add a tiny offline unit test of the pure functions (`_family_actions`, `_select_nonconflicting`, `_apply_actions`) on a synthetic mini-graph â€” the local smoke test only validates the notebook contract, not the oracle math, so logic bugs would otherwise surface only on Kaggle. Optionally surface an explicit `daughter_detectability` fraction and note that `gate_passed=True` still requires the human optimism-discount check.
5. **Housekeeping:** refresh the stale GOAL.md "RUNNING/paused" paragraph to reflect diag_014 completion.

## Recommendation
The hypothesis is testable and single-variable, the protocol inherits the frozen split with proper per-specimen reporting and leakage warnings, the runtime guards genuinely verify the effective baseline, and the cost is zero. The outstanding items are external-artifact validation and one silent-failure guard, not defects in the oracle math â€” appropriate to resolve during local smoke testing and the pre-launch artifact check.

VERDICT: PASS
