schema_version: 1
experiment_id: infra_002_v9_metrics_contract
parent: exp_001_v9_phase0_oracle
hypothesis: The v9 Phase 0 notebook emits structured gate metrics only after all oracle families are defined.
change:
  component: controller_metrics_contract
  from: console-only Phase 0 output
  to: final-code-cell metrics.json artifact
  variables_changed: 0

source_notebook: src/v9_division_aware.ipynb

validation:
  protocol: v9_phase0_oracle_train16_delta_only
  primary_metric: mean_family_a_delta_score
  seed: 0
  required_specimens: [44b6, 6bba]

budget:
  tier: 0
  expected_gpu_hours: 0.0

success:
  minimum_improvement: 0.002
  regression_threshold: -0.002
  per_specimen_max_regression: 0.0

evaluation:
  mode: gate
  gate_field: gate_0_passed

admission:
  require_claude_review: false
  methodology_valid: true
  require_reproducible_for_promotion: false

local:
  smoke_test:
    - "{python}"
    - scripts/validate_notebook.py
    - "{source_notebook}"
    - --require-metrics-contract

kaggle:
  owner: local
  slug: infra-v9-metrics-contract
  competition_sources: []
  dataset_sources: []
  required_dataset_sources: 0
  enable_gpu: false
  enable_internet: false
  is_private: true
